apiVersion: v1
kind: Service
metadata:
  name: superset
  labels:
    app: superset
    chart: superset-0.8.3
    release: superset
    heritage: Helm
  namespace: superset
spec:
  type: NodePort
  ports:
    - nodePort: 30013
      port: 8088
      protocol: TCP
      name: http
  selector:
    app: superset
    release: superset